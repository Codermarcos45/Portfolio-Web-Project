<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Social Media</title>
    <link rel="stylesheet" href="/posts.css">
</head>
<body>
    <div class="container">
        <nav>
            <div class="left-nav">
                <h1>Marcos.</h1>
            </div>
            <div class="right-nav">
                <form action="/social/posts/new">
                    <button type="submit">Add New Post</button>
                </form>
            </div>
        </nav>
        <hr>
        <div class="main">
            <% if(posts.length == 0) {%>
                <div class="no-post">
                    <h1 style="color: white;">No post found!</h1>
                </div>    
            <% } else { %>
                
                <% for(post of posts) { %>
                    <div class="box">
                        <div class="content">
                            <p><%= post.content %></p>
                        </div>
                        <img src="<%= post.image %>" alt="image">
                        <div class="btns">
                            <form action="/social/posts/<%= post.id %>">
                                <button>See in Details</button>
                            </form>
                            <form action="/social/posts/<%= post.id %>/edit">
                                <button>Edit</button>
                            </form>
                            <form action="/social/posts/<%= post.id %>?_method=DELETE" method="post">
                                <button>Delete</button>
                            </form>
                        </div>
                    </div>
                <% } %>    
            <% } %>    
        </div>
    </div>
</body>
</html>
